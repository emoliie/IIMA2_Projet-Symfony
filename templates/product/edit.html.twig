{% extends 'base.html.twig' %}

{% block title %}
    Modifier le produit - {{ product.name }}
{% endblock %}

{% block body %}
    <h1>Modifier le produit - {{ product.name }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}

    <a href="{{ path('product_show', { id: product.id }) }}" class="btn btn-secondary">Annuler</a>
{% endblock %}
